<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error - <%= appName || 'TechDine' %>
    </title>

    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        :root {
            --theme-color: <%=themeColor || '#f97316' %>;
            --theme-light: color-mix(in srgb, <%=themeColor || '#f97316' %> 80%, white);
            --theme-dark: color-mix(in srgb, <%=themeColor || '#f97316' %> 80%, black);
        }

        .theme-text {
            color: var(--theme-color);
        }

        .theme-gradient {
            background-image: linear-gradient(90deg, var(--theme-light), var(--theme-dark));
        }

        .theme-gradient:hover {
            background-image: linear-gradient(90deg, var(--theme-dark), var(--theme-color));
        }

        * {
            font-family: "Euclid Circular A", -apple-system, BlinkMacSystemFont,
                "Segoe UI", sans-serif;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .error-container {
            animation: slideDown 0.4s ease-out;
        }
    </style>
</head>

<body class="bg-gray-50 min-h-screen flex items-center justify-center px-4">

    <div class="error-container w-full max-w-md">

        <!-- Error Card -->
        <div class="bg-white rounded-2xl shadow-xl p-8 text-center">

            <!-- Error Icon -->
            <div class="mb-6 flex justify-center">
                <div class="w-20 h-20 bg-red-100 rounded-full flex items-center justify-center">
                    <svg class="w-10 h-10 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                    </svg>
                </div>
            </div>

            <!-- Error Heading -->
            <h1 class="text-2xl font-bold text-gray-900 mb-3">
                <% if (status===404) { %>
                    Page Not Found
                    <% } else { %>
                        Oops! Something went wrong
                        <% } %>
            </h1>

            <!-- Error Message -->
            <div class="mb-6">
                <div class="bg-red-50 border border-red-200 rounded-lg p-4">
                    <p class="text-sm text-red-800 font-medium">
                        <%= message || 'An unexpected error occurred' %>
                    </p>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="space-y-3">
                <button onclick="window.history.back()"
                    class="w-full theme-gradient text-white font-semibold py-3 rounded-lg shadow-md transition-all duration-200">
                    Go Back
                </button>

                <a href="/"
                    class="block w-full bg-gray-100 text-gray-700 font-semibold py-3 rounded-lg hover:bg-gray-200 transition-all duration-200">
                    Return to Home
                </a>
            </div>

            <!-- Help Text -->
            <p class="text-xs text-gray-500 mt-6">
                If this problem persists, please contact support
            </p>

            <!-- Dev Mode Error Details -->
            <% if (error && error.stack) { %>
                <details class="mt-6 text-left">
                    <summary class="cursor-pointer text-sm text-gray-600 hover:text-gray-800 font-medium">
                        🔧 Developer Details
                    </summary>
                    <div class="mt-3 bg-gray-900 text-gray-100 p-4 rounded-lg text-left overflow-auto max-h-64">
                        <p class="text-xs font-mono mb-2 text-orange-400">
                            Status: <%= status || '500' %>
                        </p>
                        <pre class="text-xs font-mono whitespace-pre-wrap"><%= error.stack %></pre>
                    </div>
                </details>
                <% } %>

        </div>

        <!-- Branding Footer -->
        <div class="mt-6 text-center">
            <div class="flex items-center justify-center space-x-2">
                <div class="w-6 h-6 theme-gradient rounded flex items-center justify-center">
                    <span class="text-white font-bold text-xs">
                        <%= (appName || 'TechDine' ).charAt(0).toUpperCase() %>
                    </span>
                </div>
                <span class="text-sm font-semibold text-gray-700">
                    <%= appName || 'TechDine' %>
                </span>
            </div>
        </div>

    </div>

</body>

</html>