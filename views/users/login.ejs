<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= appName %> - Login
  </title>

  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body {
      background-image: url('https://plus.unsplash.com/premium_photo-1661883237884-263e8de8869b?ixlib=rb-4.1.0&auto=format&fit=crop&w=1600&q=60');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
    }
  </style>
</head>

<body class="flex items-center justify-center min-h-screen p-4">

  <%- include("../includes/flash.ejs") %>

    <div class="w-full max-w-md bg-white p-8 sm:p-10 rounded-xl shadow-2xl border border-gray-200">

      <!-- LOGO + APP NAME -->
      <div class="flex flex-col items-center mb-6">
        <% if (appLogo && appLogo !=='/images/default-logo.png' ) { %>
          <img src="<%= appLogo %>" class="h-16 w-16 mb-2 object-contain" />
          <% } else { %>
            <div class="text-4xl mb-2 text-[<%= themeColor %>]">🍽️</div>
            <% } %>

              <span class="text-3xl font-bold text-gray-800">
                <%= appName %>
              </span>
      </div>

      <h2 class="text-2xl font-bold text-gray-900 text-center mb-2">Welcome Back!</h2>

      <p class="text-gray-500 text-center mb-8">
        Log in to manage your restaurant operations.
      </p>

      <!-- FORM -->
      <form action="/login" method="POST" class="space-y-6">

        <!-- EMAIL -->
        <div>
          <input type="email" name="email" required placeholder="Email Address" class="w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm
                 placeholder-gray-500 transition
                 focus:ring-2 focus:ring-[<%= themeColor %>] focus:border-[<%= themeColor %>]" />
        </div>

        <!-- PASSWORD -->
        <div>
          <input type="password" name="password" required placeholder="Password" class="w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm
                 placeholder-gray-500 transition
                 focus:ring-2 focus:ring-[<%= themeColor %>] focus:border-[<%= themeColor %>]" />
        </div>

        <!-- REMEMBER + FORGOT -->
        <div class="flex items-center justify-between text-sm">
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="checkbox" name="remember-me" class="h-4 w-4 border-gray-300 rounded
                        focus:ring-[<%= themeColor %>] text-[<%= themeColor %>]" />
            <span>Remember me</span>
          </label>

          <a href="#" class="font-medium hover:opacity-80 transition text-[<%= themeColor %>]">
            Forgot Password?
          </a>
        </div>

        <!-- LOGIN BUTTON -->
        <button type="submit" class="w-full py-3 px-4 rounded-lg text-white font-semibold shadow-lg
               transform transition hover:scale-[1.01] 
               focus:ring-4 bg-[<%= themeColor %>] focus:ring-[<%= themeColor %>]">
          Log In to Dashboard
        </button>

      </form>

      <!-- SIGNUP & HOME -->
      <div class="text-center text-sm mt-6">
        <span class="text-gray-600">Don't have an account?</span>

        <form action="/signup" method="GET" class="inline">
          <button class="font-semibold hover:opacity-80 transition text-[<%= themeColor %>]">
            Sign up
          </button>
        </form>

        <div class="mt-4">
          <a href="/techdine" class="font-medium hover:opacity-80 transition text-[<%= themeColor %>]">
            Go to Home
          </a>
        </div>
      </div>

    </div>

</body>

</html>